export default (
    state = {
      userData: {
        userName: '',
        isLoggedIn: false,
      },
    },
    action:any,
  ) => {
    switch (action.type) {
    
      default:
        return state;
    }
  };
  